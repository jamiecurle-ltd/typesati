.DS_Store
.build/
.swiftpm/
*.xcodeproj
# Built app bundle (regenerated by `make bundle`)
/typesati.app/
# Distributable archive (regenerated by `make release`)
/typesati.zip
# Build / notarization artifacts (regenerated by `make dmg` / scripts/release.sh)
/build/
/dist/
*.dmg
.wrangler/

AGENTS.md
Makefile
scripts/
